package testclasses;

import org.testng.annotations.Test;

import pageclasses.Alert;

public class AlertTest extends Alert {
@Test
public void Alerthandle() {	
Alert alert = new pageclasses.Alert();
alert.Alert(null);
}
}
